resource "aws_security_group" "alb" {
  name = "${var.name}-alb-sg"
  # 힌트: 이 SG는 VPC 안에 생성되어야 함
  
  # TODO: ALB에 들어오는 80, 443 포트의 모든 ingress 트래픽을 허용
  # 힌트: ingress 블록을 2개 만들거나 규칙을 일반화해서 80/443을 열어둘 것

  tags = {
    Name = "${var.name}-alb-sg"
  }
}