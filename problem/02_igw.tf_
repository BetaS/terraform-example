resource "aws_internet_gateway" "main" {
  vpc_id = aws_vpc.main.id

  tags = {
    Name = "${var.name}-igw"
  }
}

resource "aws_route" "internet_access" {
  # TODO: internet gateway에 모든 트래픽을 허용하는 route를 추가
  # 힌트: VPC 메인 route table에 기본 경로(0.0.0.0/0)를 추가하고, 대상은 IGW
}
